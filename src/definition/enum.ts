export enum EnumOrderStatus {
  ORDER_PLACED = "Order Placed",
  READY = "Ready",
  TAKEN = "Taken",
  DELIVERED = "Delivered"
};

export enum EnumLockStatus {
  LOCK = "lock",
  LOCKED = "locked",
  UNLOCK = "unlock",
  UNLOCKED = "unlocked"
}
